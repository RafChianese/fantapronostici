import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fantapronostici.app',
  appName: 'Fanta Pronostici',
  webDir: 'dist'
};

export default config;
